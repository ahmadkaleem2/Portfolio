apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: github-deploy-cluster-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: github-deploy-cluster-role
subjects:
- kind: ServiceAccount
  name: github-actions-sa
  namespace: default
