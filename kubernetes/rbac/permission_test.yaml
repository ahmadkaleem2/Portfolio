
apiVersion: v1
kind: ServiceAccount
metadata:
  # creationTimestamp: null
  name: test
  namespace: default
---
# apiVersion: rbac.authorization.k8s.io/v1
# kind: Role
# metadata:
#   name: no-deployments-role
#   namespace: kube-system
# rules:
# - apiGroups: ["apps"]
#   resources: ["deployments"]
#   verbs: [""]
# ---
# apiVersion: rbac.authorization.k8s.io/v1
# kind: RoleBinding
# metadata:
#   name: no-deployments-role-binding
#   namespace: kube-system
# subjects:
# - kind: ServiceAccount
#   name: test  # Replace with the service account or user you want to restrict
#   namespace: default  # Replace with the namespace of the service account
# roleRef:
#   kind: Role
#   name: no-deployments-role
#   apiGroup: rbac.authorization.k8s.io
# ---
# apiVersion: rbac.authorization.k8s.io/v1
# kind: ClusterRole
# metadata:
#   name: deployment-creator
# rules:
# - apiGroups: ["apps"]
#   resources: ["deployments"]
#   verbs: ["create", "update", "patch", "delete"]
# ---
# apiVersion: rbac.authorization.k8s.io/v1
# kind: ClusterRoleBinding
# metadata:
#   name: deployment-creator-binding
# subjects:
# - kind: ServiceAccount
#   name: test  # Replace with the service account or user you want to allow
#   namespace: default  # Replace with the namespace of the service account
# roleRef:
#   kind: ClusterRole
#   name: deployment-creator
#   apiGroup: rbac.authorization.k8s.io
---


apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: deny-deployments-in-kube-system
spec:
  validationFailureAction: enforce
  background: true
  rules:
  - name: deny-deployments-in-kube-system
    match:
      resources:
        kinds:
        - Deployment

    validate:
      deny:
        conditions:
            all:
            - key: "{{ request.object.metadata.namespace }}"
              operator: Equals
              value: "kube-system"
            - key: "{{ request.userInfo.username }}"
              operator: Equals
              value: "system:serviceaccount:default:test"
    # deny:
    #   message: "Deployments are not allowed in the kube-system namespace for the default service account."


---

apiVersion: kyverno.io/v1
# The `ClusterPolicy` kind applies to the entire cluster.
kind: ClusterPolicy
metadata:
  name: require-ns-purpose-label
# The `spec` defines properties of the policy.
spec:
  # The `validationFailureAction` tells Kyverno if the resource being validated should be allowed but reported (`Audit`) or blocked (`Enforce`).
  validationFailureAction: enforce
  # The `rules` is one or more rules which must be true.
  rules:
  - name: require-ns-purpose-label
    # The `match` statement sets the scope of what will be checked. In this case, it is any `Namespace` resource.
    match:
      any:
      - resources:
          kinds:
          - Namespace
    # The `validate` statement tries to positively check what is defined. If the statement, when compared with the requested resource, is true, it is allowed. If false, it is blocked.
    validate:
      # The `message` is what gets displayed to a user if this rule fails validation.
      message: "You must have label `purpose` with value `production` set on all new namespaces."
      # The `pattern` object defines what pattern will be checked in the resource. In this case, it is looking for `metadata.labels` with `purpose=production`.
      pattern:
        metadata:
          labels:
            purpose: production
